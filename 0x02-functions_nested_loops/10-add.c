#include "main.h"
/**
 * add- Entry point
 *@a: the integer for add.
 *@b: the seceand integer for add.
 *
 * Return: the result.
*/
int add(int a, int b)
{
	int resalt;

	resalt = a + b;

	return (resalt);
}
