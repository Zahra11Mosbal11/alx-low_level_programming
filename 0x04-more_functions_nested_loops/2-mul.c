#include "main.h"
/**
 * mul - Entry point
 *
 * @a: the first number will mul
 * @b:the seceand number will mul
 * Return: intgeger number
*/
int mul(int a, int b)
{
	int mul = a * b;

	return (mul);
}
