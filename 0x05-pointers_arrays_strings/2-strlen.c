#include "main.h"
#include <string.h>
#include <stdio.h>
/**
 * _strlen - Entry point
 *@s: array of characters
 * Return: the length of a string.
*/
int _strlen(char *s)
{
	int length;

	length = strlen(s);

	return (length);
}
