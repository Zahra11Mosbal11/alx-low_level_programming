#include "main.h"
#include <string.h>
/**
 * *_strcpy - Entry point
 *@src: cope from
 *@dest: cope to it
 * Return: char valuo
*/
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);

	return (dest);
}
